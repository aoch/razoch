// ------------------------------------------------------------------
// Functionality Tasks
// ------------------------------------------------------------------
(*) Refactor application to use separate routes for each component
    /app/epic
    /app/saga
    /app/thunk
    /app/input
    /app/clock
(*) Restrict access to application by using authentication
(*) Restrict access to components by using authentication
// ------------------------------------------------------------------



// ------------------------------------------------------------------
// CleanUp Tasks
// ------------------------------------------------------------------
(*) See if you can add depcheck to your prepush command
(*) make Redux Devtools work with Safari
(*) make Redux Devtools work with Firefox
(*) Move Jest config out of package.json into config subfolder
(*) Move Babel config out of package.json into config subfolder
(*) Create a patch for bundlesize that allows you to pass config
    in via script rather than commandline
// ------------------------------------------------------------------
